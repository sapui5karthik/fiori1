_schema-version: "3.2"
ID: salesorders
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: salesorders-destination-content
  type: com.sap.application.content
  requires:
  - name: salesorders-destination-service
    parameters:
      content-target: true
  - name: salesorders-repo-host
    parameters:
      service-key:
        name: salesorders-repo-host-key
  - name: salesorders-uaa
    parameters:
      service-key:
        name: salesorders-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: salesorders_html_repo_host
          ServiceInstanceName: salesorders-html5-service
          ServiceKeyName: salesorders-repo-host-key
          sap.cloud.service: salesorders
        - Authentication: OAuth2UserTokenExchange
          Name: salesorders_uaa
          ServiceInstanceName: salesorders-xsuaa-service
          ServiceKeyName: salesorders-uaa-key
          sap.cloud.service: salesorders
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: salesorders-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: salesorders-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - salesorders.zip
      name: salesorders
      target-path: resources/
- name: salesorders
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: salesorders-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: salesorders-destination-service
    service-plan: lite
- name: salesorders-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: salesorders-xsuaa-service
    service-plan: application
- name: salesorders-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: salesorders-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
